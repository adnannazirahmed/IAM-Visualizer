[project]
name = "cloud-iam-visualizer"
version = "0.1.0"
description = "AWS IAM Access & Privilege-Escalation Visualizer"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
