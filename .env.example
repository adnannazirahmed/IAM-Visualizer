# AWS credentials for live mode (Phase 2)
# NEVER commit actual values — use .env which is gitignored
AWS_ACCESS_KEY_ID=your-key-here
AWS_SECRET_ACCESS_KEY=your-secret-here
AWS_DEFAULT_REGION=us-east-1
